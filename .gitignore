# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
Desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Node (if tooling is added later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/

# Build output
dist/
build/
.cache/

# Environment
.env
.env.*
!.env.example

# Logs
*.log

# Python
__pycache__/
*.py[cod]

# The staged copy of the site. scripts/stage-site.sh writes it, the deploy
# uploads it, and it is every file the site serves -- so committing it once
# would double the repository and go stale the moment anything changed.
_site/

# This machine's Claude Code settings. .claude/launch.json is shared and
# committed; this one holds absolute paths that are true here and nowhere
# else -- GH_CONFIG_DIR, pinning gh to the account VectorGarden expects so a
# session working in another repository cannot flip it underneath this one.
.claude/settings.local.json
